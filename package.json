{
  "private": true,
  "scripts": {
    "test": "node --test tools/tests/*.test.mjs",
    "validate:profile-contracts": "node tools/validate-profile-contracts.mjs"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
