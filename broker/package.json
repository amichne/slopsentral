{
  "name": "@slopsentral/broker",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "bin": {
    "broker": "dist/broker.mjs"
  },
  "scripts": {
    "build": "npm run typecheck && esbuild src/runtime/cli.ts --bundle --platform=node --format=esm --target=node22 --banner:js='#!/usr/bin/env node\nimport { createRequire as __createRequire } from \"node:module\"; const require = __createRequire(import.meta.url);' --outfile=dist/broker.mjs",
    "acceptance:upstream": "KAST_EXECUTABLE=./test/kast-cli.fixture.ts tsx --test test/managed-upstream.acceptance.ts",
    "acceptance:installed": "tsx test/installed-system.acceptance.ts",
    "check": "npm run format:check && npm run typecheck && npm run check:architecture && npm run qualify && npm test && npm run build && npm run smoke",
    "check:architecture": "node scripts/check-architecture.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "qualify": "tsx src/runtime/cli.ts qualify",
    "smoke": "node dist/broker.mjs --version && KAST_EXECUTABLE=./test/kast-cli.fixture.ts node dist/broker.mjs catalog | node scripts/verify-catalog-output.mjs",
    "test": "tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sinclair/typebox": "0.34.52",
    "ajv": "8.20.0",
    "ws": "8.21.3"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "@types/ws": "8.18.1",
    "esbuild": "0.28.2",
    "prettier": "3.9.6",
    "tsx": "4.23.12",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
